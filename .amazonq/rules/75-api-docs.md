# API docs
- springdoc-openapi to generate OpenAPI.
- Document error schema + examples; include correlation/trace fields.
- Version endpoints if breaking changes; keep /v3/api-docs + Swagger UI enabled (non-prod).
