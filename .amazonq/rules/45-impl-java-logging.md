# Java logging
- private static final Logger log = LoggerFactory.getLogger(X.class);
- INFO milestones, WARN recoverable, ERROR failures.
- Parameterized key/value logs; no secrets or payload dumps.
- Replace System.out/printStackTrace.
