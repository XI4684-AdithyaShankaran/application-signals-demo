# Using Amazon Q (meta)
- Name the exact file path + rule IDs.
- Keep scope tiny (one file/method).
- List 3–5 concrete edits.
- Output = unified diff only.
- No package/public API changes.
- Split large diffs (>300 LOC) into batches.
