# Build & dependencies
- Pin versions; avoid dynamic ranges. Maven Enforcer on.
- Fail build on known CVEs (OWASP Dependency-Check/Trivy).
- Reproducible builds; small Docker images; cache-friendly layers.
