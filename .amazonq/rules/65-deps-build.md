# Build & dependencies
- Pin versions; avoid dynamic ranges.
- Maven Enforcer for reproducible builds; deterministic JARs.
- Fail build on known CVEs (OWASP Dependency-Check/Trivy).
- Keep Docker image small; cache-friendly layers.
